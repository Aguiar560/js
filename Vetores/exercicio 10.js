//Crie um vetor de 7 números inteiros e calcule a soma de todos os números do vetor.
    let vetor= [10,33,55,76,80,2,7]
    let soma = 0
        for(let i=0;i<vetor.length;i++){
            soma+=vetor[i]
        }
    console.log(soma)