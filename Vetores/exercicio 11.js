//Crie um vetor com 5 valores e escreva uma função que retorne um novo vetor com todos os valores multiplicados por 2.
let vetor = [55,6,80,77,20]
let multi = []
    for(let i=0;i<vetor.length;i++){
        multi[i]=vetor[i]*2
    }
    console.log(multi)